DX9 SDK 2004 December version
	essential include/library files only in /include9
	this SDK because it was the last one where D3DX was static library.

DX11 + DX12 from Windows SDK 14316
	essential include files in /include
	dxguid.lib for d3d11 in libs

Windows 8.1 SDK version
	D3DCompiler_47.dll in libs and libs64

Also DirectInput header/libraries from VS2003. The PlatformSDK that comes with VS2008 does not have it anymore.
